function addNum(num1: number, num2: number, phrase: string) {
    const result = num1 + num2;
    console.log(phrase + result);
}

const number1 = 10;
const number2 = 2.8;
const resultPhrase = "The result is: ";

addNum(number1, number2, resultPhrase);